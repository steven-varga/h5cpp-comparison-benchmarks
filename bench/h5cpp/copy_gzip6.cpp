/* copy_gzip6.cpp — vargalabs h5cpp: element-by-element IEX tick copy with gzip-6.
 *
 * Usage: ./copy-gzip6-h5cpp <input.h5> <output.h5>
 *
 * Streams source ticks via h5::view<tick_t> (C++20 ranges over chunked
 * datasets), then writes each tick through h5::pt_t + h5::append with an
 * 8-thread worker-pool FAPL (h5::threads{8}).  The pool activates
 * pool_pipeline_t, offloading chunk compression to workers.
 *
 * The tick_t compound type descriptor is generated by the h5cpp-compiler
 * (see bench/h5cpp/generated.h).
 */

#include <h5cpp/all>
#include "tick.h"
#include "generated.h"

#include <cstdio>
#include <cstdlib>
#include <ctime>
#include <csignal>
#include <unistd.h>

#define DATASET_NAME    "2026-05-28"
#define GROUP_NAME      "irts"
#define OUT_CHUNK_ELEMS 4194304ULL   /* 4 M elements  ≈ 100 MiB per chunk */
#define TIMEOUT_SEC     300
#define PROGRESS_EVERY  100000000ULL

static volatile int g_timed_out = 0;

static void on_alarm(int sig) {
    (void)sig;
    g_timed_out = 1;
}

static double monotonic_seconds() {
    struct timespec ts;
    clock_gettime(CLOCK_MONOTONIC, &ts);
    return ts.tv_sec + ts.tv_nsec * 1e-9;
}

int main(int argc, char* argv[]) {
    if (argc != 3) {
        fprintf(stderr, "Usage: %s <input.h5> <output.h5>\n", argv[0]);
        return 1;
    }

    const char* in_path  = argv[1];
    const char* out_path = argv[2];

    signal(SIGALRM, on_alarm);
    alarm(TIMEOUT_SEC);
    setbuf(stdout, NULL);

    try {
        /* ── Open source ──────────────────────────────────────────────────── */
        h5::fd_t in_fd = h5::open(in_path, H5F_ACC_RDONLY);
        h5::ds_t in_ds = h5::open(in_fd, "/" GROUP_NAME "/" DATASET_NAME);

        h5::sp_t file_space = h5::get_space(in_ds);
        h5::current_dims dims;
        h5::get_simple_extent_dims(file_space, dims);
        hsize_t nelem = dims[0];

        h5::dt_t<tick_t> dtype;
        size_t type_size = H5Tget_size(static_cast<hid_t>(dtype));

        printf("Source: %s  elements=%zu  type_size=%zu  total=%.2f GiB  timeout=%ds\n",
               in_path, static_cast<size_t>(nelem), type_size,
               static_cast<double>(nelem * type_size) / (1024.0 * 1024.0 * 1024.0),
               TIMEOUT_SEC);

        /* ── Create output with threaded FAPL ─────────────────────────────── */
        h5::fapl_t fapl = h5::threads{8};
        h5::fd_t out_fd = h5::create(out_path, H5F_ACC_TRUNC, h5::default_fcpl, fapl);

        h5::pt_t pt = h5::create<tick_t>(out_fd, "/" GROUP_NAME "/" DATASET_NAME,
                h5::max_dims{H5S_UNLIMITED},
                h5::chunk{OUT_CHUNK_ELEMS} | h5::gzip{6});

        /* ── Stream-read + element-by-element append ──────────────────────── */
        double t0 = monotonic_seconds();
        size_t total_copied = 0;
        size_t next_report  = PROGRESS_EVERY;

        volatile float checksum = 0.0f;
        for (const auto& tick : h5::view<tick_t>(in_ds)) {
            if (g_timed_out) break;
            checksum += tick.price;
            h5::append(pt, tick);
            ++total_copied;

            if (total_copied >= next_report) {
                double el = monotonic_seconds() - t0;
                printf("  progress: %zu ticks  %.1f s  %.2f Mticks/s\n",
                       total_copied, el, (total_copied / el) / 1e6);
                next_report += PROGRESS_EVERY;
            }
        }
        (void)checksum;

        h5::flush(pt);

        double elapsed = monotonic_seconds() - t0;
        double gib = static_cast<double>(total_copied * type_size) / (1024.0 * 1024.0 * 1024.0);

        if (g_timed_out) {
            printf("TIMEOUT after %.1f s — partial: %zu ticks (%.3f GiB)\n",
                   elapsed, total_copied, gib);
            return 2;
        } else {
            printf("Done: %zu ticks in %.3f s  %.3f GiB  %.2f MiB/s  %.2f Mticks/s\n",
                   total_copied, elapsed, gib,
                   (gib * 1024.0) / elapsed, (total_copied / elapsed) / 1e6);
        }

    } catch (const std::exception& e) {
        fprintf(stderr, "Error: %s\n", e.what());
        return 1;
    }

    return 0;
}
